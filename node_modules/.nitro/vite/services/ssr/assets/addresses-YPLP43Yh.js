import { jsxs, jsx } from "react/jsx-runtime";
import { F as addressesQO, G as addAddress, H as deleteAddress } from "./router-B46y8PhA.js";
import { Link } from "@tanstack/react-router";
import { useSuspenseQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { u as useServerFn } from "./useServerFn-DL2oePlL.js";
import { N as Navbar, F as Footer } from "./Footer-4N6S3dWq.js";
import { toast } from "sonner";
import "@supabase/supabase-js";
import "react-hot-toast";
import "@vercel/analytics/react";
import "./server-BfnmGY_x.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "zod";
import "./auth-middleware-DBKcBFEt.js";
import "node:crypto";
import "lucide-react";
function AddressesPage() {
  const {
    data: addresses
  } = useSuspenseQuery(addressesQO);
  const qc = useQueryClient();
  const addFn = useServerFn(addAddress);
  const delFn = useServerFn(deleteAddress);
  const [busy, setBusy] = useState(false);
  async function onAdd(e) {
    e.preventDefault();
    setBusy(true);
    try {
      const fd = new FormData(e.currentTarget);
      await addFn({
        data: {
          full_name: String(fd.get("full_name")),
          phone: String(fd.get("phone")),
          line1: String(fd.get("line1")),
          line2: String(fd.get("line2") ?? ""),
          city: String(fd.get("city")),
          state: String(fd.get("state")),
          pincode: String(fd.get("pincode")),
          country: "India"
        }
      });
      e.currentTarget.reset();
      qc.invalidateQueries({
        queryKey: ["my-addresses"]
      });
      toast.success("Address saved");
    } catch (err) {
      toast.error(err instanceof Error ? err.message : "Failed");
    } finally {
      setBusy(false);
    }
  }
  async function onDelete(id) {
    await delFn({
      data: {
        id
      }
    });
    qc.invalidateQueries({
      queryKey: ["my-addresses"]
    });
  }
  return /* @__PURE__ */ jsxs("div", { className: "bg-background min-h-screen", children: [
    /* @__PURE__ */ jsx(Navbar, {}),
    /* @__PURE__ */ jsx("section", { className: "pt-32 pb-16", children: /* @__PURE__ */ jsxs("div", { className: "mx-auto max-w-[1000px] px-6", children: [
      /* @__PURE__ */ jsx(Link, { to: "/account", className: "text-[11px] tracking-luxury uppercase text-mute hover:text-charcoal", children: "← Back to account" }),
      /* @__PURE__ */ jsx("h1", { className: "mt-6 font-serif text-3xl md:text-4xl tracking-luxury", children: "Addresses" }),
      /* @__PURE__ */ jsxs("div", { className: "mt-10 grid md:grid-cols-2 gap-12", children: [
        /* @__PURE__ */ jsxs("div", { children: [
          /* @__PURE__ */ jsx("h2", { className: "text-[11px] tracking-luxury uppercase text-mute mb-6", children: "Saved" }),
          addresses.length === 0 ? /* @__PURE__ */ jsx("p", { className: "text-mute text-sm", children: "No saved addresses." }) : /* @__PURE__ */ jsx("div", { className: "space-y-4", children: addresses.map((a) => /* @__PURE__ */ jsxs("div", { className: "p-5 hairline-b text-sm", children: [
            /* @__PURE__ */ jsx("div", { className: "font-serif text-base", children: a.full_name }),
            /* @__PURE__ */ jsxs("div", { className: "text-mute mt-1", children: [
              a.line1,
              a.line2 ? `, ${a.line2}` : "",
              /* @__PURE__ */ jsx("br", {}),
              a.city,
              ", ",
              a.state,
              " ",
              a.pincode,
              /* @__PURE__ */ jsx("br", {}),
              a.phone
            ] }),
            /* @__PURE__ */ jsx("button", { onClick: () => onDelete(a.id), className: "mt-3 text-[11px] tracking-luxury uppercase text-mute hover:text-charcoal", children: "Remove" })
          ] }, a.id)) })
        ] }),
        /* @__PURE__ */ jsxs("form", { onSubmit: onAdd, className: "space-y-4", children: [
          /* @__PURE__ */ jsx("h2", { className: "text-[11px] tracking-luxury uppercase text-mute mb-6", children: "Add new" }),
          /* @__PURE__ */ jsx(Input, { name: "full_name", label: "Full name", required: true }),
          /* @__PURE__ */ jsx(Input, { name: "phone", label: "Phone", required: true, type: "tel" }),
          /* @__PURE__ */ jsx(Input, { name: "line1", label: "Address", required: true }),
          /* @__PURE__ */ jsx(Input, { name: "line2", label: "Apt / Suite" }),
          /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-3 gap-3", children: [
            /* @__PURE__ */ jsx(Input, { name: "city", label: "City", required: true }),
            /* @__PURE__ */ jsx(Input, { name: "state", label: "State", required: true }),
            /* @__PURE__ */ jsx(Input, { name: "pincode", label: "PIN", required: true })
          ] }),
          /* @__PURE__ */ jsx("button", { type: "submit", disabled: busy, className: "w-full bg-charcoal text-ivory py-4 text-[11px] tracking-luxury uppercase hover:bg-gold transition-colors disabled:opacity-50", children: busy ? "Saving…" : "Save address" })
        ] })
      ] })
    ] }) }),
    /* @__PURE__ */ jsx(Footer, {})
  ] });
}
function Input({
  name,
  label,
  type = "text",
  required = false
}) {
  return /* @__PURE__ */ jsxs("label", { className: "block", children: [
    /* @__PURE__ */ jsx("span", { className: "block text-[11px] tracking-wide-2 uppercase text-mute mb-2", children: label }),
    /* @__PURE__ */ jsx("input", { name, type, required, className: "w-full bg-transparent hairline-b py-2 text-sm focus:outline-none focus:border-charcoal" })
  ] });
}
export {
  AddressesPage as component
};
