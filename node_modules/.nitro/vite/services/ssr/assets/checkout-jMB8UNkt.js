import { jsxs, jsx } from "react/jsx-runtime";
import { useRouter, Link } from "@tanstack/react-router";
import { useState, useRef, useEffect } from "react";
import { u as useServerFn } from "./useServerFn-DL2oePlL.js";
import { N as Navbar, F as Footer } from "./Footer-4N6S3dWq.js";
import { j as createSsrRpc, u as useCart, K as createOrder, d as supabase } from "./router-B46y8PhA.js";
import { f as formatINR } from "./format-Sk5HC8SH.js";
import { h as createServerFn } from "./server-BfnmGY_x.js";
import { z } from "zod";
import { r as requireSupabaseAuth } from "./auth-middleware-DBKcBFEt.js";
import { toast } from "sonner";
import "lucide-react";
import "@tanstack/react-query";
import "@supabase/supabase-js";
import "react-hot-toast";
import "@vercel/analytics/react";
import "node:crypto";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
const createRazorpayOrder = createServerFn({
  method: "POST"
}).middleware([requireSupabaseAuth]).validator((input) => z.object({
  orderId: z.string().uuid()
}).parse(input)).handler(createSsrRpc("a6f1a7df2dd032270b33ae7f01da2576971e1b7652c3d182f28f0f762ce126d4"));
const verifyRazorpayPayment = createServerFn({
  method: "POST"
}).middleware([requireSupabaseAuth]).validator((input) => z.object({
  orderId: z.string().uuid(),
  razorpay_order_id: z.string().min(1).max(100),
  razorpay_payment_id: z.string().min(1).max(100),
  razorpay_signature: z.string().min(1).max(200)
}).parse(input)).handler(createSsrRpc("5e2c6a85ce8b9f3a92cd9b0a9b4d8f015d9ec2fa0b30eb31f8605ecef9f67199"));
function useRazorpayScript() {
  const [loaded, setLoaded] = useState(typeof window !== "undefined" && !!window.Razorpay);
  useEffect(() => {
    if (typeof window === "undefined" || window.Razorpay) {
      setLoaded(true);
      return;
    }
    const s = document.createElement("script");
    s.src = "https://checkout.razorpay.com/v1/checkout.js";
    s.async = true;
    s.onload = () => setLoaded(true);
    document.body.appendChild(s);
    return () => {
      s.remove();
    };
  }, []);
  return loaded;
}
function CheckoutPage() {
  const cart = useCart();
  const router = useRouter();
  const createOrderFn = useServerFn(createOrder);
  const createRpFn = useServerFn(createRazorpayOrder);
  const verifyRpFn = useServerFn(verifyRazorpayPayment);
  const rpReady = useRazorpayScript();
  const [submitting, setSubmitting] = useState(false);
  const idemRef = useRef(`chk_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 10)}`);
  const shipping = cart.subtotal >= 25e3 || cart.subtotal === 0 ? 0 : 250;
  const tax = Math.round(cart.subtotal * 0.05);
  const total = cart.subtotal + shipping + tax;
  async function onSubmit(e) {
    e.preventDefault();
    if (cart.items.length === 0) return;
    setSubmitting(true);
    try {
      const {
        data: session
      } = await supabase.auth.getSession();
      if (!session.session) {
        toast.error("Please sign in to complete your order.");
        router.navigate({
          to: "/auth",
          search: {
            redirect: "/checkout"
          }
        });
        return;
      }
      const fd = new FormData(e.currentTarget);
      const address = {
        full_name: String(fd.get("full_name") ?? ""),
        phone: String(fd.get("phone") ?? ""),
        email: String(fd.get("email") ?? ""),
        line1: String(fd.get("line1") ?? ""),
        line2: String(fd.get("line2") ?? ""),
        city: String(fd.get("city") ?? ""),
        state: String(fd.get("state") ?? ""),
        pincode: String(fd.get("pincode") ?? ""),
        country: "India"
      };
      const order = await createOrderFn({
        data: {
          items: cart.items.map((i) => ({
            productId: i.productId,
            quantity: i.quantity
          })),
          shipping_address: address,
          idempotencyKey: idemRef.current
        }
      });
      let rp;
      try {
        rp = await createRpFn({
          data: {
            orderId: order.id
          }
        });
      } catch (err) {
        console.error(err);
        toast.error("Online payment unavailable. Your order is saved as pending — our team will contact you.");
        cart.clear();
        router.navigate({
          to: "/checkout/success",
          search: {
            o: order.order_number
          }
        });
        return;
      }
      if (!rpReady || typeof window === "undefined" || !window.Razorpay) {
        toast.error("Payment widget failed to load. Please refresh and try again.");
        return;
      }
      const checkout = new window.Razorpay({
        key: rp.keyId,
        amount: rp.amount,
        currency: rp.currency,
        name: "IRAYA",
        description: `Order ${rp.orderNumber}`,
        order_id: rp.razorpayOrderId,
        prefill: {
          name: address.full_name,
          email: address.email,
          contact: address.phone
        },
        theme: {
          color: "#1a1a1a"
        },
        handler: async (resp) => {
          try {
            await verifyRpFn({
              data: {
                orderId: order.id,
                razorpay_order_id: resp.razorpay_order_id,
                razorpay_payment_id: resp.razorpay_payment_id,
                razorpay_signature: resp.razorpay_signature
              }
            });
            cart.clear();
            router.navigate({
              to: "/checkout/success",
              search: {
                o: order.order_number
              }
            });
          } catch (err) {
            console.error(err);
            toast.error("Payment received but verification failed. We'll reconcile shortly.");
          }
        },
        modal: {
          ondismiss: () => {
            toast.info("Payment cancelled. Your order is saved as pending.");
          }
        }
      });
      checkout.open();
    } catch (err) {
      console.error(err);
      toast.error(err instanceof Error ? err.message : "Could not place order");
    } finally {
      setSubmitting(false);
    }
  }
  return /* @__PURE__ */ jsxs("div", { className: "bg-background text-foreground min-h-screen", children: [
    /* @__PURE__ */ jsx(Navbar, {}),
    /* @__PURE__ */ jsx("section", { className: "pt-32 pb-12 hairline-b", children: /* @__PURE__ */ jsxs("div", { className: "mx-auto max-w-[1200px] px-6 text-center", children: [
      /* @__PURE__ */ jsx("div", { className: "text-[11px] tracking-luxury uppercase text-mute", children: "Checkout" }),
      /* @__PURE__ */ jsx("h1", { className: "mt-4 font-serif text-4xl md:text-5xl tracking-luxury", children: "Finalise your order" })
    ] }) }),
    cart.items.length === 0 ? /* @__PURE__ */ jsxs("div", { className: "py-20 text-center", children: [
      /* @__PURE__ */ jsx("p", { className: "text-mute mb-6", children: "Your bag is empty." }),
      /* @__PURE__ */ jsx(Link, { to: "/collection", className: "text-[11px] tracking-luxury uppercase hover:text-gold", children: "Browse the collection" })
    ] }) : /* @__PURE__ */ jsx("section", { className: "py-16", children: /* @__PURE__ */ jsxs("div", { className: "mx-auto max-w-[1200px] px-6 grid lg:grid-cols-3 gap-12", children: [
      /* @__PURE__ */ jsxs("form", { onSubmit, className: "lg:col-span-2 space-y-6", children: [
        /* @__PURE__ */ jsx("h2", { className: "font-serif text-2xl", children: "Shipping details" }),
        /* @__PURE__ */ jsxs("div", { className: "grid sm:grid-cols-2 gap-4", children: [
          /* @__PURE__ */ jsx(Field, { name: "full_name", label: "Full name", required: true }),
          /* @__PURE__ */ jsx(Field, { name: "phone", label: "Phone", required: true, type: "tel" })
        ] }),
        /* @__PURE__ */ jsx(Field, { name: "email", label: "Email", required: true, type: "email" }),
        /* @__PURE__ */ jsx(Field, { name: "line1", label: "Address line 1", required: true }),
        /* @__PURE__ */ jsx(Field, { name: "line2", label: "Address line 2" }),
        /* @__PURE__ */ jsxs("div", { className: "grid sm:grid-cols-3 gap-4", children: [
          /* @__PURE__ */ jsx(Field, { name: "city", label: "City", required: true }),
          /* @__PURE__ */ jsx(Field, { name: "state", label: "State", required: true }),
          /* @__PURE__ */ jsx(Field, { name: "pincode", label: "Pincode", required: true })
        ] }),
        /* @__PURE__ */ jsx("button", { type: "submit", disabled: submitting, className: "w-full bg-charcoal text-ivory px-8 py-4 text-[11px] tracking-luxury uppercase hover:bg-gold transition-colors disabled:opacity-50", children: submitting ? "Processing…" : `Pay ${formatINR(total)} securely` }),
        /* @__PURE__ */ jsx("p", { className: "text-[11px] text-mute text-center", children: "Payment is processed by Razorpay. Your card details never touch our servers." })
      ] }),
      /* @__PURE__ */ jsxs("aside", { className: "bg-secondary p-8 h-fit", children: [
        /* @__PURE__ */ jsx("h2", { className: "font-serif text-xl mb-6", children: "Your bag" }),
        /* @__PURE__ */ jsx("div", { className: "space-y-3 mb-6", children: cart.items.map((it) => /* @__PURE__ */ jsxs("div", { className: "flex gap-3 text-sm", children: [
          /* @__PURE__ */ jsx("div", { className: "w-14 h-16 shrink-0 bg-ivory overflow-hidden", children: /* @__PURE__ */ jsx("img", { src: it.imageUrl, alt: it.name, className: "h-full w-full object-cover" }) }),
          /* @__PURE__ */ jsxs("div", { className: "flex-1 min-w-0", children: [
            /* @__PURE__ */ jsx("div", { className: "font-serif text-sm truncate", children: it.name }),
            /* @__PURE__ */ jsxs("div", { className: "text-[11px] text-mute", children: [
              "× ",
              it.quantity
            ] })
          ] }),
          /* @__PURE__ */ jsx("div", { className: "text-sm", children: formatINR(it.price * it.quantity) })
        ] }, it.productId)) }),
        /* @__PURE__ */ jsxs("div", { className: "pt-4 hairline-t space-y-2 text-sm", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex justify-between", children: [
            /* @__PURE__ */ jsx("span", { className: "text-mute", children: "Subtotal" }),
            /* @__PURE__ */ jsx("span", { children: formatINR(cart.subtotal) })
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex justify-between", children: [
            /* @__PURE__ */ jsx("span", { className: "text-mute", children: "Shipping" }),
            /* @__PURE__ */ jsx("span", { children: shipping === 0 ? "Free" : formatINR(shipping) })
          ] }),
          /* @__PURE__ */ jsxs("div", { className: "flex justify-between", children: [
            /* @__PURE__ */ jsx("span", { className: "text-mute", children: "GST" }),
            /* @__PURE__ */ jsx("span", { children: formatINR(tax) })
          ] })
        ] }),
        /* @__PURE__ */ jsxs("div", { className: "mt-4 pt-4 hairline-t flex justify-between font-serif text-xl", children: [
          /* @__PURE__ */ jsx("span", { children: "Total" }),
          /* @__PURE__ */ jsx("span", { children: formatINR(total) })
        ] })
      ] })
    ] }) }),
    /* @__PURE__ */ jsx(Footer, {})
  ] });
}
function Field({
  name,
  label,
  type = "text",
  required = false
}) {
  return /* @__PURE__ */ jsxs("label", { className: "block", children: [
    /* @__PURE__ */ jsxs("span", { className: "block text-[11px] tracking-wide-2 uppercase text-mute mb-2", children: [
      label,
      required && " *"
    ] }),
    /* @__PURE__ */ jsx("input", { name, type, required, className: "w-full bg-transparent hairline-b py-2 text-sm focus:outline-none focus:border-charcoal" })
  ] });
}
export {
  CheckoutPage as component
};
